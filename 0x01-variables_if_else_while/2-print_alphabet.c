#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
int lowercase = 97;
while (lowercase <= 122)
{
putchar(lowercase);
lowercase++;
}
putchar('\n');
return (0);
}
